# DESIGN：市场广度（McClellan / NYMO 口径）能否用于 Confluence 策略

> **来源**：`quantrift_index_future` 2026-08-14 的研究交接。
> 那边把广度这条线在**期货侧**做完了（判负/搁置，见文末「已经关掉的」），
> 换靶子到本项目的信号台账后出现一个**未预期的、量级可观的观察**。
>
> **本文是 DESIGN，不是结论。按 `AGENTS.md`「工作阶段必须分离」，
> 本文不实现任何改动，也不主张现在就用。**

---

## 0. 一页结论

在本项目 `logs/backfill_signal_log.csv` 的 **7,374 条 Confluence 信号 / 1,152 个交易日**上：

> **前一交易日的市场广度越低，当天触发的 Confluence 信号事后表现越差。**
> 广度最低五分位 **+0.169 R** vs 最高五分位 **+0.571 R**，端点差 **−0.402 R**。

| 检验 | 结果 |
|---|---|
| 日期块置换 p | **0.0000**（Holm 跨全部被检验策略后仍 < 0.01）|
| 两个广度 universe 复现 | ✅ NYSE 普通股 −0.402 / 全市场普通股 −0.323（p=0.0014）|
| 剔除 2020 | −0.412（**不是 COVID 驱动**）|
| 再剔除 2022 | −0.450 |
| 逐年方向 | **5/6 同向**（2022 熊市是唯一反号年 +0.908）|
| **控制前一日大盘收益** | **仍在**：组内端点差 5/5 同向、均值 **−0.209 R** |

**⚠️ 但它是 post-hoc。** 那一轮实验**明确把 Confluence 预注册为
「中性、不做方向预测、不进判据」**（预注册的命题是别的，且已被证伪）。
事后发现它显著，按纪律**只能算待验证的假设**。

**方向的含义**：广度**高**时 Confluence 更好 ⇒ 这是**参与度确认**
（广度的惯例用法），**不是**「超卖反转」那一类。这与 Confluence 本身
偏趋势/共振的性质一致——市场参与面宽时，多指标共振更容易兑现。

---

## 1. 冻结口径（**不许再扫**）

任何验证都必须用下面这个一字不改的定义。**在历史样本上再调任何一个自由参数，
这个假设就作废了**（过拟合螺旋）。

```
广度序列   McClellan ratio-adjusted 振荡器
           RANA = (adv − dec) / (adv + dec)
           osc  = (EMA19(RANA) − EMA39(RANA)) × 1000

universe   主口径 = NYSE 普通股（经典 NYMO）
           对照   = 全市场普通股
           两者均由 research/build_breadth_by_universe.py 产出

特征       osc[t−1]        ← 前一交易日；广度收盘后才可得，t 日盘中信号只能用 t−1
分档       按**交易日**切五分位（不按信号切，否则信号多的日子主导分档边界）
目标       该信号的 r_mult（已实现 R 倍数）
统计       **按日期分块**的置换检验
```

### ⚠️ 两条方法纪律，比阈值更重要

**① 有效样本是「不同日期数」，不是「信号数」。**
同一天几十个标的一起触发，共用同一个广度值，且当日结果高度相关（市场同涨同跌）。
Confluence 是 7,374 条信号但只有 **1,152 个交易日**（每日均 6.4 条）。
逐信号置换会把有效样本虚报 6-8 倍、p 值严重偏小。**必须整天一起换组。**

**② 报 p 值必须同时报可检出下限。**
上游项目 2026-08-14 曾因漏算下限而对另一个结论过度声称，当天被推翻。

---

## 2. 这个假设要怎么被验证（预注册，谁跑都一样）

### 2.1 先说清楚：**纯向前验证不可行，不能拿它当门**

按实测 `r_mult` 标准差 ≈ 2.20 计算 80% 功效、α=0.05 所需样本：

| 要检出的效应 | 每档所需**交易日** | 五档合计 | 折合年数 |
|---|---|---|---|
| −0.402 R（原始端点差） | 469 | 2,345 | **约 9.3 年** |
| −0.209 R（控制大盘收益后） | 1,738 | 8,690 | **约 34 年** |

⇒ **「等向前样本攒够再说」等于永远不做决定。**
向前监测有价值，但它的角色是**持续确认**，不是**准入门槛**。

### 2.2 真正独立、且**现在就能做**的两个检验

关键在于：上游那轮**只看过「按时间分档」这一个切面**。
下面两个维度**从未被看过**，因此在统计上是干净的独立检验。

#### 检验 A：**跨标的折半**（首选）

```
把 101 个标的用固定种子随机折半（seed=20260814），两半各自独立重跑同一口径。
通过条件（跑之前写死）：
  A1 两半的端点差**同号为负**
  A2 两半各自 日期块置换 p < 0.10
  A3 两半端点差之比落在 [0.5, 2.0]（量级不能差一倍以上）
任一不过 ⇒ 该假设很可能是少数标的驱动的，判负。
```

#### 检验 B：**跨周期拆分**

```
把信号按 tf 拆成 1h / 4h / 1d 三组，各自独立重跑。
通过条件：
  B1 三组中 ≥2 组端点差为负
  B2 至少一组 p < 0.10
  B3 **1d 组必须同号** —— 广度是日频量，若它对日线信号无效而只对 1h 有效，
     机制上说不通，更像噪声。
```

> **A 与 B 必须在同一次提交里一起跑完并同时报告**，不许挑一个报。

### 2.3 「所以能不能用」——经济意义检验（A、B 都过之后才做）

```
反事实：把广度最低五分位（约 20% 的交易日）的 Confluence 信号
        ① 完全跳过  ② 半仓
        对照 = 原样全做
通过条件：
  C1 总实现 R 提升 ≥ 10%
  C2 **放弃的信号数占比 ≤ 提升幅度的 2 倍**（排除「少做自然少亏」的假象）
  C3 逐年 ≥ 5/8 年同向
```

⚠️ **C 不通过不代表广度没信息**，只代表「按这个用法用不上」——
这两件事上游刚因为混淆而返工过一次，见 `LESSONS.md`（那边）2026-08-14
「不要用『用不上』去否决『有没有』」。

### 2.4 向前监测（持续，不设门）

每月把新增的 Confluence 信号按同一口径归档，报一次端点差与累计天数。
**唯一会触发行动的情形是符号翻转并持续 ≥ 6 个月** ——
那说明这个关系不稳定，应当直接关档。

---

## 3. ⚠️ 阻塞项：广度序列目前**停在 2026-08-07**，不会自己更新

交付的 `breadth_common_*_daily.csv` 覆盖 **2018-05-01 → 2026-08-07**，
由 `research/build_breadth_by_universe.py` 从一份**一次性的 Databento
XNAS.ITCH 导出**（`~/Downloads/XNAS-20260810-JLNA94QEHN`，2,079 个交易日）构建。
**它不是一个每日更新的管道。**

检验 A / B 用历史数据就能跑完，**不受此影响**。
但 §2.3 的落地与 §2.4 的向前监测需要每日 A/D 数据。三条路：

| 方案 | 说明 | 代价 |
|---|---|---|
| **① 读 `quantrift_options-lab` 的 `market_breadth_daily`**（推荐） | 该表已**每日自动落库**，Polygon grouped daily + **point-in-time 普通股参照**（自带 ETF 排除），字段含 `advances/declines/unchanged` 与 `exchange_breakdown`（**可拆出 NYSE-only，正好对上主口径**），覆盖 XNAS+XNYS+XASE，coverage ≈ 98.8% | 跨项目**只读** Postgres 依赖；且该表 **2026-07-30 才开始收集**，无历史 |
| ② 本项目自建 Polygon 采集 | 完全独立 | 需要本项目自己的 Polygon key 与额度，且与 ① 重复造轮子 |
| ③ 定期重新导出 Databento | 与历史序列口径完全一致 | 人工、易漏、有数据成本 |

**推荐 ①**：历史用交付的 CSV（Databento 口径），向前用 `market_breadth_daily`。
⚠️ **两段口径不同**（Databento XNAS 场内 vs Polygon 三所普通股），
**拼接处必须显式标注，且不得跨口径直接比较端点差** ——
正确做法是两段各自计算、并列报告。

---

## 4. 已经关掉的（**不要重做**）

上游 2026-08-14 已用 8 个用法 × 5 个前瞻期 × 4 个标的（NQ/ES/QQQ/SPY）×
块置换 + Holm 共 160 格点做完，全部不通过：

| 用法 | 裁决 |
|---|---|
| 尾部超卖/超买（±70、5%/95% 分位）→ 指数收益 | **搁置**：8 年只有约 22 次独立回调，效应 +3pp 而可检出下限 +4~5pp，**测不出来**。重开需 2018 年前数据把独立事件提到 50+ 次，**不是换阈值** |
| 价格/广度背离 | **搁置**：最好一格是 QQQ 10 日 +1.74%（未校正 p=0.0514，n=78），Holm 后归零 |
| 零线穿越 | **判负**：n=192 分布均匀，效应 0.0~0.3pp |
| 作趋势系统的入场否决门 | **判负**：MAR −48.5% / −36.1% |
| **作均值回归类策略的择时过滤** | **判负**：在本项目台账上 RSI2 −0.003 / RSI2_IBS +0.061 / MRVL −0.578 R **方向不一致**，且与突破类**不相反** ⇒ 预注册命题被证伪 |

**最后一行尤其要记住**：直觉上「广度低=超卖=MR 该更好」，**实测不成立**。
本文这个假设的方向是**反的**（广度高时趋势/共振策略更好）。

---

## 5. 交付物

```
research/build_breadth_by_universe.py          广度构建（含内置验收，验收不过拒绝产出数据）
research/exp_breadth_stock_signals.py          发现该现象的实验（口径与判据写在文件头）
research/data_external/
    breadth_common_nyse_daily.csv              NYSE 普通股（主口径）2018-05-01 → 2026-08-07
    breadth_common_all_daily.csv               全市场普通股（对照）
    nasdaq_symbol_directory_20260814.txt       Nasdaq 官方证券目录快照（免费无 key，ETF 标记 + 上市所）
```

复现（已在本项目验证跑通，系统 python3.11 + pandas 3.0.3）：

```bash
/opt/homebrew/bin/python3.11 research/exp_breadth_stock_signals.py
```

重建广度序列（需 Databento 导出在位，约几分钟）：

```bash
/opt/homebrew/bin/python3.11 research/build_breadth_by_universe.py
```

> `build_breadth_by_universe.py` 内置验收：15 个必剔 ETF 必须全部判对、
> 10 只普通股必须保留、NYSE 口径必须含 JPM/XOM/KO 且不含 AAPL/MSFT。
> **验收不过则拒绝产出数据** —— 上一版用行为判据（R²）剔 ETF 时
> 15 个只认出 1 个，正是这道门当场拦下了一份分类全错的广度。

---

## 6. 建议的执行顺序

1. **检验 A（跨标的折半）+ 检验 B（跨周期拆分）** —— 数据在手，一次跑完，同时报告
2. A/B 任一不过 ⇒ 写进 `LEARNING.md` 判负关档，本文废止
3. A/B 都过 ⇒ 决定 §3 的数据管道，再做 §2.3 经济意义检验
4. §2.3 过了才谈要不要接进 `alert_engine.py`；**接之前必须是一次显式的人工拍板**

**在第 4 步之前，不改任何生产文件、不改任何信号逻辑。**
