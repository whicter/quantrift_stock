# WIKI.md — quantrift_stock 策略文档

## 策略概述

继承自 `quantrift_index_future` 的 ConfluenceStrategy，原参数针对 NQ/ES 期货优化。
本项目将其应用于美股个股和 ETF，仅发信号告警，不执行下单。

## 信号评分系统（满分 6）

| 分项 | 多头条件 | 空头条件 |
|---|---|---|
| B1 UTBot | close > utTS | close < utTS |
| B2 SSL | close > BBMC & ssl1 (或 buyCont) | close < BBMC & ssl1 (或 sellCont) |
| B3 RSI | RSI > 50 | RSI < 50 |
| B4 MACD | MACD线 > 信号线 | MACD线 < 信号线 |
| B5 Squeeze | sqzVal > 0 且上升 | sqzVal < 0 且下降 |
| B6 CD背离 | 底背离 | 顶背离 |

入场条件：`bull_score >= min_score`（默认5）且 ADX ≥ adx_threshold（默认25）且放量

## 出场逻辑（staged_tp=True 模式）

```
stage 1（满仓）→ 止损：utTS穿越
                → TP1：entry + 1×ATR（平 34%）→ stage 2
stage 2（持66%）→ 止损：utTS穿越
                → TP2：entry + 2×ATR（平剩余50%）→ stage 3
stage 3（持33%）→ sslExit 跟踪止盈（吃大趋势）
```

## 参数说明

| 参数 | 默认值 | 说明 |
|---|---|---|
| `min_score` | 5 | 入场最低得分（满分6） |
| `adx_threshold` | 25.0 | ADX趋势强度过滤（股票推荐20-30） |
| `ut_key` | 1.5 | UTBot止损宽度（越大越松，回撤大但不易被扫） |
| `atr_tp1_mult` | 1.0 | TP1位置 = entry ± 1×ATR |
| `atr_tp2_mult` | 2.0 | TP2位置 = entry ± 2×ATR |
| `tp1_portion` | 0.34 | TP1平仓比例（1手时无效，整数取整为0） |
| `ssl_len` | 20 | SSL主线周期 |
| `exit_len` | 15 | sslExit追踪线周期 |

## RSI2-Trend 变体（2026-08-15 新增）

**不是独立策略**，是 RSI2 引擎的参数覆盖，通过 `RSI2_PARAMS` 注入，无新增代码：

```python
{"use_rs_filter": False, "max_hold_bars": 30}
```

| 关掉的东西 | 为什么 |
|---|---|
| RS vs QQQ 过滤 | 要求标的跑赢纳指，对非科技股（药企/医疗/宽基红利ETF）本就无意义 |
| 10 根 bar 时间止损 → 放长到 30 | 持续趋势型标的的行情需要时间展开，10 根就踢出等于反复砍在半路 |

**适用判据（事前注册，不可事后修改）**：长期趋势型 = 收盘价在 200SMA 上方的时间
占比 ≥65% 且 年化波动 ≤40%。

**已接入 1d 路由**：LLY / CSCO / ISRG / AVDV / VYM / DGRO / TSM / CRWD

**⚠️ 八个标的共用完全相同的参数，严禁逐标的调参。** 这个变体的全部证据来自一次
泛化检验：配置冻结后原封不动套到全部 223 个可评估标的，达标的 10 个里 8 个属于
事前预测的趋势型（80% vs 全样本基准 30%）。一旦对某个标的微调参数，这个论据就
退化成"八次各自的样本内寻优"，等同于当年把 LLY/SLS/USO 送进实盘又失效的网格优化。

## 期权纸面模拟（2026-08-15 新增，绝不下单）

不是新策略，是**度量层**：信号发出后按 mid 价买一张 ATM 期权，正股策略出场时平掉，
用来回答"同一个信号，做期权比做正股划算多少"。

| 项 | 规则 |
|---|---|
| 报价源 | yfinance 实时期权链（信号当下拉取） |
| 方向 | 做多→call，做空→put |
| 行权价 | 最接近现价的 ATM |
| 到期 | `clamp(持仓上限交易日 × 3.5, 30, 60)`，优先月度（第三个周五），**30 为硬下限** |
| 成交 | 主口径 **mid→mid**；同时记保守口径（买ask/卖bid）作对照 |
| 准入 | 白名单 84 个标的，条件是目标到期处 **OI≥50 且有有效买价**，不看价差 |
| 时效 | 只接 **30 分钟内**发出的信号 |
| 出场行情 | 与引擎同源（`alert_engine.fetch_bars`），**不读本地 CSV** |
| 金额 | 每笔预算 `BUDGET_USD=750`，记 `contracts`/`cost_usd`/`pnl_usd` |
| 归因 | 入场与出场两端都记 IV 与标的价 |

> **⚠️ 2026-09-03 之前的 136 笔全部作废**（`contaminated=1`）。当时
> `review_core.evaluate` **分不清「走满持仓上限」和「行情数据到头了」**，两者
> 都返回「时间止损」，于是每个仓位开仓后一两根 bar 就被判出场（中位持仓 6 小时、
> 75% 不到 8 小时）。同一个 bug 也让正股纸面组合把真实的 +5.70% 记成 -8.16%
> （484 笔里 68% 只持有 1 根 bar）。基于这批样本的一切结论均已撤回。
> 详见 CLAUDE.md「review_core 的『数据走完 ≠ 到持仓上限』」。

**五条来自实测的硬约束**：
- **DTE 必须封顶**。LLY 持仓30天若机械按3.5倍外推到105DTE，OI 只剩39；
  而它34DTE处 OI 有602。远月省的 theta 远不抵流动性损失。
- **DTE 也必须有下限（30天）**。理由是流动性不是 theta：近月 vs 次月 ATM 实测
  PLTR OI 618→4,004、HOOD 208→3,161，价差双双腰斩。
- **优先月度到期**。实测 CRWD 同一时点：周度价差16.0%/OI99，月度8.4%/OI233；
  MU 周度 OI 34、月度 3,777。
- **必须在信号当下开仓**。用几天后的期权报价对应几天前的信号是时点错配，
  算出来的盈亏无意义（实测 MU 正股+1.36R vs 期权-1.7%，差异全部来自错配）。
- **出场判定必须与信号同源**。信号用 yfinance 生成、出场却读本地 CSV，而本地
  由会静默失败的夜间 IB 任务保鲜——25 个标的曾停在 8/21，之后开的仓永远出不了场。

**测流动性只能在盘中测**：盘后做市商撤单放宽，同一张合约价差是盘中的 3 倍
（MU 2.3%→7.9%）。`options_whitelist.py` 直接拒绝在非交易时段运行。

**长持仓策略与期权天然不搭**：持仓越久越需要远月，远月越没流动性。
AVDV/VYM/FDVV/DGRO 这类红利宽基 ETF 各到期几乎无市场（OI 0-46），只做正股。

## 与期货版本的差异

| 项目 | 期货（quantrift_index_future） | 股票（quantrift_stock） |
|---|---|---|
| 合约乘数 | MNQ=$2/点，MES=$5/点 | 无（$1/点 = 每股） |
| `contract_size` | 2（MNQ） | 1 |
| 交易时段 | 近24h（周日-周五） | 09:30-16:00 ET |
| 滚动合约 | ContFuture 自动处理 | 不需要 |
| 保证金 | 期货固定 | Reg T 50%（日内25%） |
| 执行 | 实盘下单 | **仅 Telegram 告警** |
| clientId | 1 | 2 |

## 回测结论

> 持续更新，详见 `LEARNING.md`

| 标的 | 周期 | Sharpe | MaxDD | 胜率 | 笔数 | 备注 |
|---|---|---|---|---|---|---|
| （待填入） | | | | | | |

## 数据说明

- **告警运行时（2026-07-27 起混合架构）**：yfinance 为主（15分钟延时）；1d 近期缺 bar 用本地 IB 填补；yfinance 拉空时整段回退本地 IB；本地 IB 数据由夜间任务（交易日 14:00 PT）自动保鲜。引擎不直连 IB
- **完整 bar 语义**：信号只在完整 bar 收盘后产生（盘中无 1d 新信号），且仅在新鲜窗口内发出（1h=4小时/4h=12小时/1d=30小时），不补发陈旧信号
- **离线历史回测/回填**：优先使用 `data/{SYMBOL}_{TF}.csv`；IB Gateway 仅用于显式补拉并合并，4h 由 1h 重采样
- **4h**：由 1h 重采样（OHLCV 聚合规则：O=first, H=max, L=min, C=last, V=sum）
- **⚠️ 采样锚点差异（2026-07-25 核实，回测 vs 实盘的已知偏差源）**：
  - IB 1h bar 用**整点锚**（09:30/10:00/11:00/…/15:00，每交易日 7 根）；yfinance 1h bar 用**半点锚**（09:30/10:30/…/15:30，每交易日 7 根）。同名"1h bar"覆盖的时间片错开 30 分钟。
  - 4h 更明显：离线 CSV（`fetch_ib_data.resample_4h`）用 `closed="left", label="left"` → bar 标 08:00/12:00；实时 `alert_engine.fetch_bars` 用 `closed="right", label="right"` → bar 标 12:00/16:00。同一天 NVDA 2026-07-23 两边收盘价分别为 208.18/208.69（离线）与 209.63/208.70（实时）。
  - **性质**：不是数据错误，是回测与实盘看到的 K 线边界不同。统一约定会改变实时信号本身（属策略行为变化），需单独评估后再决定，当前仅记录在案。
- **日线价格准确性已验证**：IB 用 `ADJUSTED_LAST` + `useRTH=True`，yfinance 用 `auto_adjust=True`，口径一致。2026-07-25 抽查 NVDA/JPM/KO 近 5 个交易日收盘价，两边差异 **0.000%**。
- **RTH 下每日 bar 数（时间止损换算基准）**：1h = 7 根/交易日；**4h = 2 根/交易日**（08:00、12:00）；1d = 1 根/交易日。任何"最长持仓 N 根"的表述都必须按此换算成交易日，不能按日历 24 小时除。
- **数据存储**：`data/{SYMBOL}_{TF}.csv`；用 `data_audit.py --write` 查看覆盖、陈旧度和补拉计划
- **4h 的 yfinance 窗口 = 730d（2026-09-03 起）**：4h 由 1h 重采样，`60d` 只得
  ~119 根 4h bar，而 `check_rsi2_signal` 开头 `if len(df) < 210: return None`
  （SMA200 要 200 根）——**36 条 RSI2 4h 路由从上线起一条信号都没发过**。
  730d 下有 ~1,707 根。Confluence 4h 不受影响（只要 50 根）。
- **⚠️ 夜间 IB 保鲜会静默失败（2026-09-03 发现并加告警）**：Gateway 会在批量拉取
  中途掉线，之后每个标的都打印「请求失败：Not connected」，但脚本照常跑完、退出
  0、pm2 认为成功。实测 108 个已路由标的里 **24 个的日线、25 个的 1h/4h 停在
  8/21 整整 13 天**。本地数据既是引擎的补缺兜底源，也是 `review_core` 复盘的
  价格来源，陈旧会**同时污染信号和复盘**——那些标的的信号在复盘里直接算不出 R。
  `fetch_ib_data.py` 现在按文件 mtime 判定每个标的是否真的更新，失败率 >10% 推 TG。
- **2026-07-24 运行状态**：2026-07-18 发现的 IB Gateway US 历史数据 farm `ushmds` 断连已解决（经用户批准重启 Gateway + 手机 2FA 批准），IB 补拉恢复正常；72 个 `symbol × tf` 文件已用 `fetch_ib_data.py --merge` 重新覆盖至 2026-07-23，来源为 `ib`（此前 2026-07-18 的 yfinance 合并记录仅作历史参考）
- **ETF 扫描器数据独立**：47 个 ETF/基准日线不包含在上述 72 文件中；2026-07-18 曾有 43 个陈旧，2026-07-24 Gateway 恢复后已用 `fetch_etf_data.py` 全部回补，覆盖至 2026-07-23（VIX 至 07-24），ETF 轮动扫描现可直接使用最新数据
- **SNDK**：2025-02-20 重新上市，历史数据有限（约1年）

## 信号去重与复盘基准（2026-09-03 起）

**想法级去重**：引擎此前没有持仓状态，只要入场条件还成立就每根 bar 重新播报同一
个交易想法——90 天 470 条信号其实只有 **210 个独立想法**（NVDA/RSI2 连播 5 天、
QQQ 连播 12 条）。回测里一个想法只开一次仓，实盘却把每次重播都当独立交易记进
复盘，而重播是同一波行情里越来越晚的入场点，均 R 被系统性拉低。

判定用 `review_core` 的出场逻辑（`_is_repeat_idea` + `data/.open_ideas.json`），
与回测「一次交易」的定义完全对齐，不是「隔几天算新的」这种拍脑袋阈值。
重播仍写进 `signal_log`（`is_repeat=1`，账本记全）但**不推送、不计入复盘**。

效果：推送 **-42%**；Confluence 均 R 从 +0.224 回到 **+0.289**（回测期望 +0.285）
——所谓「Confluence 衰减」完全是这个口径造出来的假象。

**复盘基准 = 同期回测**：`expectations.json` 的 `mean_r` 是十年长期均值，拿它判定
最近 90 天等于把「这一季行情本来就不好」误读成「策略衰减」。同期回测（只算真正
开过仓的行）Confluence **-0.163**、RSI2 **-0.060**，而实盘去重后 Confluence
**+0.289** 其实是**跑赢**同期回测的。用长期均值当基准会在每段低于平均的行情里批量
误报红灯 → 触发自动降级 → 正好把策略在最不该关的时候关掉。同期样本 <10 笔才退回
长期均值。

## 告警格式

信号按板块合并推送（2026-08-15 起），每个板块一条消息：

```
📦 半导体（2 条）  ⚠️ 同板块集中：做多2

📊 NVDA 1h 做多信号
  ...

📊 MU 1h 做多信号
  ...
```

单条信号本体格式：

```
📊 NVDA 1h 做多信号
  价格: $887.5  ATR: $18.2
  Bull得分: 5/6  ADX: 32.4
  TP1: $905.7  TP2: $923.9  SL(utTS): $851.2
  时间: 2026-06-20 14:00 ET
```

## 信号的执行语义（人工执行时如何落地）

一条信号发出后，"什么时候该出场"由三个条件先到先算：

| 出场条件 | 依据 | 消息里有没有 |
|---|---|---|
| 触及 TP | `tp1`/`tp2`（Confluence 有；RSI2/MR 无固定 TP） | ✅ 有 |
| 触及 SL | `sl`（入场时刻快照值） | ✅ 有 |
| 时间止损 | 从**下一根 bar** 起数满 N 根仍未触发 TP/SL，按当根收盘价平仓 | ✅ 2026-07-25 起标注具体 bar 数与交易日 |

**时间止损的 N 取自该信号自身的策略参数**（`signal_log.csv` 的 `params_json.max_hold_bars`），不是全局固定值。换算成交易日要按上文"RTH 下每日 bar 数"：4h 的 10 根 = 5 个交易日，不是 2 个。

**降级路由（2026-08-15 起）**：`logs/demoted_routes.json` 里的组合仍会计算并记入
`signal_log`（便于观察是否恢复），但不推送 Telegram。降级由"连续N周红灯 + 用最新
数据重验证也未通过"双重确认后产生——单看实盘红灯不降级，因为 5-20 笔的样本完全
可能只是市场风格切换。

**执行账本（2026-08-15 起）**：信号发出后，在 Telegram 直接发「接 NVDA 176.5」/
「平 NVDA 182.3」/「跳过 TSLA」即可记录真实成交，`execution_ledger.py` 会自动匹配
最近信号、计算滑点与实际收益，写入 `logs/execution_log.csv`。发「账本」查看汇总。
这是系统唯一能看到"真金白银"口径的地方——所有 R 值都只是理论值。

**SL 是入场快照，不是固定值**：Confluence 的 `SL(utTS)` 是 utTS 追踪线在入场时刻的读数，之后每根 bar 都会向有利方向移动；RSI2/MR 的 SL 同理（ATR 追踪）。消息只推送入场时的初始值，不再推送后续更新——人工执行时若要完全复刻策略，需自行按 `atr_trail_mult` 滚动上移止损。
